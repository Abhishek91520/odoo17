[options]
; This is the password that allows database operations:
; admin_passwd = admin
admin_passwd = admin

db_host = ${DB_HOST}
db_port = ${DB_PORT}
db_user = ${DB_USER}
db_password = ${DB_PASSWORD}

addons_path = /opt/render/project/src/addons

default_productivity_apps = True
